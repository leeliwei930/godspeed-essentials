scopes:
    producer_categories:
        label: Producer Category
        modelClass: GodSpeed\FlametreeCMS\Models\ProducerCategory
        conditions:  id in (select producer_id from godspeed_flametreecms_producer_categories_pivot where producer_category_id in (:filtered))
        nameFrom: name
